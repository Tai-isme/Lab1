---
phase: "08"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - PRN232.LAB_1.API/Controllers/SemesterController.cs
  - PRN232.LAB_1.API/Controllers/CourseController.cs
  - PRN232.LAB_1.API/Controllers/SubjectController.cs
  - PRN232.LAB_1.API/Controllers/StudentController.cs
  - PRN232.LAB_1.API/Controllers/EnrollmentController.cs
autonomous: true
requirements:
  - API-09
  - API-10
user_setup: []

must_haves:
  truths:
    - "All 5 Delete endpoints return ApiResponse<object> with {success: true, message: 'Deleted successfully', data: {...}}"
    - "All 25 controller actions (5 entities x 5 actions) have [ProducesResponseType(StatusCodes.Status500InternalServerError)] attribute"
    - "Swagger UI shows 500 response schema for every endpoint"
  artifacts:
    - path: "PRN232.LAB_1.API/Controllers/SemesterController.cs"
      provides: "Semester CRUD with fixed Delete response and 500 attributes"
      contains: "ApiResponse<object>.Ok"
    - path: "PRN232.LAB_1.API/Controllers/CourseController.cs"
      provides: "Course CRUD with fixed Delete response and 500 attributes"
      contains: "ApiResponse<object>.Ok"
    - path: "PRN232.LAB_1.API/Controllers/SubjectController.cs"
      provides: "Subject CRUD with fixed Delete response and 500 attributes"
      contains: "ApiResponse<object>.Ok"
    - path: "PRN232.LAB_1.API/Controllers/StudentController.cs"
      provides: "Student CRUD with fixed Delete response and 500 attributes"
      contains: "ApiResponse<object>.Ok"
    - path: "PRN232.LAB_1.API/Controllers/EnrollmentController.cs"
      provides: "Enrollment CRUD with fixed Delete response and 500 attributes"
      contains: "ApiResponse<object>.Ok"
  key_links:
    - from: "PRN232.LAB_1.API/Controllers/*.cs"
      to: "PRN232.LAB_1.API/Models/ApiResponse.cs"
      via: "ApiResponse<object>.Ok() factory method in Delete endpoints"
      pattern: "ApiResponse<object>\\.Ok"
    - from: "PRN232.LAB_1.API/Controllers/*.cs"
      to: "PRN232.LAB_1.API/Filters/ResponseEnvelopeFilter.cs"
      via: "Filter detects ApiResponse<T> and skips double-wrapping (existing IsGenericType check)"
      pattern: "IsGenericType.*ApiResponse"
---

<objective>
Fix all 5 Delete endpoints to return explicit `ApiResponse<object>` instead of anonymous objects, and add `[ProducesResponseType(StatusCodes.Status500InternalServerError)]` to all 25 controller actions for Swagger 500 documentation.

Purpose: Ensure Delete responses use the consistent ApiResponse envelope (D-03) and Swagger documents 500 errors for all endpoints (D-04, API-09, API-10).
Output: Updated Delete methods + 500 attributes across all 5 controllers
</objective>

<execution_context>
@C:/Users/hieu_tai/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/hieu_tai/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-th-m-phase-fix-l-i-n-y/08-CONTEXT.md
@PRN232.LAB_1.API/Models/ApiResponse.cs
@PRN232.LAB_1.API/Filters/ResponseEnvelopeFilter.cs
@PRN232.LAB_1.API/Controllers/SemesterController.cs
@PRN232.LAB_1.API/Controllers/CourseController.cs
@PRN232.LAB_1.API/Controllers/SubjectController.cs
@PRN232.LAB_1.API/Controllers/StudentController.cs
@PRN232.LAB_1.API/Controllers/EnrollmentController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Delete endpoints to return ApiResponse<object> in all 5 controllers</name>
  <files>
    PRN232.LAB_1.API/Controllers/SemesterController.cs
    PRN232.LAB_1.API/Controllers/CourseController.cs
    PRN232.LAB_1.API/Controllers/SubjectController.cs
    PRN232.LAB_1.API/Controllers/StudentController.cs
    PRN232.LAB_1.API/Controllers/EnrollmentController.cs
  </files>
  <read_first>
    - PRN232.LAB_1.API/Models/ApiResponse.cs (ApiResponse<T>.Ok factory — use this exact shape)
    - PRN232.LAB_1.API/Filters/ResponseEnvelopeFilter.cs (confirms filter skips ApiResponse<T> via IsGenericType check)
  </read_first>
  <action>
In ALL 5 controllers (SemesterController, CourseController, SubjectController, StudentController, EnrollmentController), modify the Delete method:

**Current pattern (in every controller):**
```csharp
return Ok(new { message = "Deleted successfully" });
```

**Replace with (in every controller):**
```csharp
return Ok(ApiResponse<object>.Ok(new { message = "Deleted successfully" }));
```

**Required import:** Add `using PRN232.LAB_1.API.Models;` to the top of each controller file (if not already present).

**Exact change per controller:** Find the line `return Ok(new { message = "Deleted successfully" });` in the Delete method and replace it with `return Ok(ApiResponse<object>.Ok(new { message = "Deleted successfully" }));`.

Do NOT change any other part of the Delete method. The `if (!deleted) return NotFound();` guard stays unchanged.
Do NOT change any other method in any controller in this task.
  </action>
  <acceptance_criteria>
    - All 5 controllers contain `ApiResponse<object>.Ok(new { message = "Deleted successfully" })` in their Delete method
    - All 5 controllers contain `using PRN232.LAB_1.API.Models;`
    - No controller contains `return Ok(new { message = "Deleted successfully" });` (anonymous object form eliminated)
    - `if (!deleted)` / `return NotFound()` guard unchanged in all 5 controllers
    - No other methods modified in any controller
  </acceptance_criteria>
  <verify>
    <automated>dotnet build PRN232.LAB_1.API/PRN232.LAB_1.API.csproj</automated>
  </verify>
  <done>All 5 Delete endpoints return ApiResponse<object> envelope, project builds</done>
</task>

<task type="auto">
  <name>Task 2: Add 500 Internal Server Error status code attribute to all 25 controller actions</name>
  <files>
    PRN232.LAB_1.API/Controllers/SemesterController.cs
    PRN232.LAB_1.API/Controllers/CourseController.cs
    PRN232.LAB_1.API/Controllers/SubjectController.cs
    PRN232.LAB_1.API/Controllers/StudentController.cs
    PRN232.LAB_1.API/Controllers/EnrollmentController.cs
  </files>
  <read_first>
    - PRN232.LAB_1.API/Controllers/SemesterController.cs (existing [ProducesResponseType] pattern to follow)
  </read_first>
  <action>
In ALL 5 controllers, add `[ProducesResponseType(StatusCodes.Status500InternalServerError)]` to EVERY action method (GetAll, GetById, Create, Update, Delete — 5 actions per controller = 25 total).

**Placement rule:** Add the 500 attribute AFTER all existing `[ProducesResponseType]` attributes on each action, but BEFORE the method signature line.

**Example for SemesterController GetAll (apply this pattern to all 25 actions):**
```csharp
[HttpGet]
[ProducesResponseType(StatusCodes.Status200OK)]
[ProducesResponseType(StatusCodes.Status500InternalServerError)]    // ← ADD THIS
public async Task<IActionResult> GetAll([FromQuery] PagedQuery query)
```

**Example for SemesterController Create:**
```csharp
[HttpPost]
[ProducesResponseType(StatusCodes.Status201Created)]
[ProducesResponseType(StatusCodes.Status400BadRequest)]
[ProducesResponseType(StatusCodes.Status500InternalServerError)]    // ← ADD THIS
public async Task<IActionResult> Create([FromBody] SemesterRequest request)
```

**Actions per controller (5 each):**
- `GetAll` — currently has only 200 → add 500
- `GetById` — currently has 200 + 404 → add 500
- `Create` — currently has 201 + 400 → add 500
- `Update` — currently has 200 + 404 + 400 → add 500
- `Delete` — currently has 200 + 404 → add 500

Do NOT modify any other attributes, method signatures, or method bodies. Only add the single 500 attribute line.
  </action>
  <acceptance_criteria>
    - Each of the 5 controller files contains exactly 5 occurrences of `StatusCodes.Status500InternalServerError` (one per action)
    - Total across all controllers: 25 occurrences of `StatusCodes.Status500InternalServerError`
    - No existing [ProducesResponseType] attributes removed or modified
    - Method signatures unchanged
    - Method bodies unchanged
  </acceptance_criteria>
  <verify>
    <automated>dotnet build PRN232.LAB_1.API/PRN232.LAB_1.API.csproj</automated>
  </verify>
  <done>All 25 actions have 500 status code attribute, Swagger will document 500 responses, project builds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller → ResponseEnvelopeFilter | Delete now returns ApiResponse<object> — filter must recognize and skip re-wrapping (existing IsGenericType check handles this) |
| Swagger → Client | 500 status code documentation exposes error possibility — informational only, no security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | Delete endpoints | mitigate | Delete returns `ApiResponse<object>.Ok(new { message = "Deleted successfully" })` — no entity data, IDs, or internal state exposed in deletion confirmation. |
| T-08-05 | Repudiation | All controllers | accept | No audit logging in this phase. 500 status code documentation in Swagger does not create audit trail. Out of scope for this lab. |
| T-08-06 | Information Disclosure | Swagger 500 docs | accept | `[ProducesResponseType(500)]` only documents that 500 errors are possible — does not expose internal error details. Actual error content controlled by ExceptionHandlingMiddleware (T-08-01). |
</threat_model>

<verification>
- Project builds without errors: `dotnet build`
- All 5 Delete methods return `ApiResponse<object>.Ok(...)` not anonymous `new { message = ... }`
- All 5 controllers have `using PRN232.LAB_1.API.Models;`
- All 25 action methods (5 controllers x 5 actions) have `[ProducesResponseType(StatusCodes.Status500InternalServerError)]`
- No other controller code modified (method bodies, signatures, other attributes unchanged)
</verification>

<success_criteria>
- All 5 Delete endpoints return explicit ApiResponse<object> envelope matching the response contract
- All 25 controller actions document 500 status code for Swagger UI
- Project builds successfully
- ResponseEnvelopeFilter correctly skips double-wrapping for ApiResponse<object> Delete responses
</success_criteria>

<output>
After completion, create `.planning/phases/08-th-m-phase-fix-l-i-n-y/08-02-SUMMARY.md`
</output>
